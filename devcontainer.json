{
    "name": "MedorCoin Blockchain",
    "image": "mcr.microsoft.com/devcontainers/cpp:ubuntu-24.04",
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libleveldb-dev libjemalloc-dev liburing-dev libgflags-dev libsnappy-dev liblz4-dev libzstd-dev libbz2-dev zlib1g-dev libssl-dev libsecp256k1-dev nlohmann-json3-dev && bash setup_leveldb_pc.sh",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cpptools",
                "ms-vscode.cmake-tools"
            ]
        }
    }
}
